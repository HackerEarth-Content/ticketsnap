server {
    listen 80;
    root /usr/share/nginx/html;

    location /dashboard/ {
        proxy_pass http://backend:8000;
    }
    location /api/ {
        proxy_pass http://backend:8000;
    }
    location / {
        try_files $uri /index.html;
    }
}
