services:
  backend:
    build: .
    env_file: .env  # compose parses quotes correctly, unlike `docker run --env-file`
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
